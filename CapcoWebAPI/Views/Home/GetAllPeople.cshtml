@model List<CoreApiClient.Entities.People>

@{
    ViewData["Title"] = "Todas as Personagens";
}

    <h3>Todas as personagens ordenadas por quantidade de filmes e por ordem alfabética</h3>

<div class="text-left">

    @foreach (var item in Model)
    {
        <p>@item.name</p>
    }

</div>

